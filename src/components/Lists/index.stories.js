import React from "react";

import Lists from ".";

export default {
  title: "Components/Lists",
  argTypes: {},
};

const Template = () => <Lists />;

export const Default = Template.bind({});

Default.args = {};
