import React from "react";

import Sort from ".";

export default {
  title: "Components/Sort",
};

function Template() {
  return <Sort />;
}

export const Default = Template.bind({});

Default.args = {};
