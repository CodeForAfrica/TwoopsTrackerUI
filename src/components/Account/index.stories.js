import React from "react";

import Account from ".";

export default {
  title: "Components/Account",
};

const Template = () => {
  return <Account />;
};

export const Default = Template.bind({});

Default.args = {};
