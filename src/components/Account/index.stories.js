import React from "react";

import Account from ".";

export default {
  title: "Components/Account",
};

function Template() {
  return <Account />;
}

export const Default = Template.bind({});

Default.args = {};
