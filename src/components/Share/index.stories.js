import React from "react";

import Share from ".";

export default {
  title: "Components/Share",
  argTypes: {},
};

const Template = () => <Share />;

export const Default = Template.bind({});

Default.args = {};
