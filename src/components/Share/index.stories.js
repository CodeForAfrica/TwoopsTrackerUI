import React from "react";

import Share from ".";

export default {
  title: "Components/Share",
  argTypes: {},
};

function Template() {
  return <Share />;
}

export const Default = Template.bind({});

Default.args = {};
