import React from "react";

import Investigations from ".";

export default {
  title: "Components/Investigations",
  argTypes: {},
};

const Template = () => <Investigations />;

export const Default = Template.bind({});

Default.args = {};
