import React from "react";

import Filter from ".";

export default {
  title: "Components/Filter",
  argTypes: {},
};

function Template() {
  return <Filter />;
}

export const Default = Template.bind({});

Default.args = {};
