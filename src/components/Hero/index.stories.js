import React from "react";

import Hero from ".";

export default {
  title: "Components/Hero",
  argTypes: {},
};

const Template = () => <Hero />;

export const Default = Template.bind({});

Default.args = {};
