import React from "react";

import Search from ".";

export default {
  title: "Components/Search",
  argTypes: {},
};

function Template() {
  return <Search />;
}

export const Default = Template.bind({});

Default.args = {};
